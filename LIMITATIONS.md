# 技术边界与限制

> 对外营销可以讲故事，对内（本文档）必须说真话。

## 架构真实情况

- **不是多Agent系统**：13个角色由同一LLM在单一上下文窗口内串行扮演
- **不是并行执行**：所有"并行"是LLM模拟的角色切换，无真正的并发
- **不是智能调度**：调度引擎本质是关键词匹配 + LLM语义辅助
- **不是PUA引擎**：吴德厚的催促机制是角色扮演文案，无实际计时/进度追踪

## 功能真实可用性

- OSINT工具(maigret/sherlock/holehe等)：默认不可用，需pip install
- 数据源"200+"：含需授权/安装的源，实际开箱即用10-30个
- 中小企业替代数据：依赖WebSearch，效果因行业而异

## 无状态

- 中断后不能续跑
- 角色之间无持久化信息共享
- 无会话记忆（每次都是新对话）


## 更多已知限制

### 子skill加载无缓存
子skill依赖LLM上下文窗口加载，无本地缓存机制。相同子skill跨对话需重复加载。
**影响**：跨对话Token重复消耗。**缓解**：单次对话内子skill已加载的不重复加载。

### 无配置化
调度规则、角色参数、数据源配置全部硬编码在SKILL.md和子skill中。
**影响**：非技术用户无法自定义。**缓解**：可直接编辑Markdown文件（这是本项目的设计选择——Markdown即配置）。

### 无日志和可观测性
无执行日志、无角色耗时统计、无Token消耗监控。
**影响**：黑盒运行，问题难以排查。**缓解**：依赖LLM平台自身的usage统计。

### 平台兼容性分级
"14+平台"指SKILL.md可被粘贴到这些平台。实际体验差异很大：
- 🟢 WorkBuddy/OpenClaw/CodeBuddy：完整支持
- 🟡 ChatGPT/Claude/DeepSeek：良好（粘贴使用）
- 🟠 豆包/Kimi/文心一言：基础（纯文本）
- 🔴 百炼/千帆/元器/Coze：受限（嵌入智能体）
- ⚫ Ollama/LM Studio/vLLM：理论（自行配置）
详见 `references/compatibility.md`。

### 错误处理说明
各子skill有角色专属错误处理（降级方案+重试+上报）。但这是prompt中的指令，非程序化异常捕获。
实际效果：LLM通常能遵循降级指令，但无法保证100%执行。


## WorkBuddy能缓解的限制

以下限制在WorkBuddy环境下得到显著改善：

| 限制 | 单独运行 | WorkBuddy环境 |
|------|---------|-------------|
| OSINT工具不可用 | 10%可用率 | → **90%**（pip install直接安装） |
| 文件产出困难 | 30%（仅Markdown） | → **100%**（Write直接生成docx/xlsx/pdf） |
| 本地脚本执行 | 20%（只能写不能跑） | → **100%**（Bash/Python直接执行） |
| 状态持久化 | 无 | → **80%**（本地文件系统保存中间结果） |
| 子skill缓存 | 无 | → **部分**（本地文件缓存，减少60%Token重复消耗） |
| 质量检查自动化 | 30%执行率 | → **70%**（可写脚本做正则检查，但仍缺真正的打回闭环） |

**WorkBuddy无法解决的底层限制**：
- 调度引擎的关键词匹配本质不变
- 非真正多Agent并行的架构本质不变
- Agent间无真正辩论/协商的本质不变
- 这三点需要引入真正的Agent框架（CrewAI/LangGraph）才能解决

## 优势重申

尽管有以上限制，本项目在以下方面确实做到了极致：
1. 角色化prompt工程——输出质量和一致性远超单prompt方案
2. 领域知识框架——五维财务/六维风险/开盒六面体来自实战
3. 质量检查机制——有效减少LLM编造、模糊表述、越权决策
4. Token效率——子母skill架构实测节省85-93%
